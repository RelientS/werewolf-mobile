# 🎨 Werewolf Mobile - 设计文档

## UI/UX 设计

### 配色方案
- **主色**: 深蓝色 (#1a1a2e) - 神秘感
- **强调色**: 金色 (#ffd700) - 贵族感
- **狼人色**: 红色 (#e74c3c) - 危险
- **村民色**: 绿色 (#2ecc71) - 安全
- **中性色**: 灰色 (#95a5a6) - 平衡

### 页面设计

#### 1. 首页 (HomeScreen)
```
┌─────────────────────────┐
│   🐺 WEREWOLF           │
│                         │
│   [快速开始]            │
│   [创建房间]            │
│   [加入房间]            │
│   [排行榜]              │
│   [设置]                │
└─────────────────────────┘
```

#### 2. 房间大厅 (RoomScreen)
```
┌─────────────────────────┐
│ ← 房间: #ABC123         │
├─────────────────────────┤
│ 玩家列表:               │
│ ┌─────────────────────┐ │
│ │ 👤 Alice    [准备] │ │
│ │ 🤖 AI-1     [准备] │ │
│ │ 🤖 AI-2     [等待] │ │
│ └─────────────────────┘ │
│                         │
│ 配置:                   │
│ 狼人: 2  预言家: 1      │
│ 医生: 1  平民: 3        │
│                         │
│ [准备]     [开始游戏]   │
└─────────────────────────┘
```

#### 3. 游戏界面 (GameScreen)
```
┌─────────────────────────┐
│ 🌙 夜晚 - 第1天         │
├─────────────────────────┤
│ 存活玩家:               │
│ ┌───┬───┬───┬───┐      │
│ │ 👤│🤖│🤖│🤖│      │
│ │ 1 │ 2 │ 3 │ 4 │      │
│ └───┴───┴───┴───┘      │
│                         │
│ 你的角色: 🐺 狼人        │
│                         │
│ 💬 聊天/发言           │
│ ┌─────────────────────┐ │
│ │ AI-2: 我觉得...     │ │
│ └─────────────────────┘ │
│                         │
│ 行动: [选择目标]        │
└─────────────────────────┘
```

#### 4. 投票界面
```
┌─────────────────────────┐
│ 🗳️ 投票阶段            │
├─────────────────────────┤
│ 请投票放逐一名玩家:     │
│                         │
│ ┌───────────────────┐  │
│ │ 👤 Alice          │  │
│ │ 得票: ■■□□ (2)  │  │
│ └───────────────────┘  │
│ ┌───────────────────┐  │
│ │ 🤖 AI-1           │  │
│ │ 得票: ■□□□ (1)  │  │
│ └───────────────────┘  │
│                         │
│ 你的投票: [选择]        │
│                         │
│ 倒计时: 30秒            │
└─────────────────────────┘
```

## 交互流程

### 1. 游戏开始流程
```
用户打开 App
  ↓
首页 (选择模式)
  ↓
创建/加入房间
  ↓
等待玩家准备 (AI 自动准备)
  ↓
开始游戏
  ↓
分配角色
  ↓
进入游戏循环
```

### 2. 游戏循环
```
┌→ 夜晚阶段
│    ↓
│  各角色行动 (狼人杀人、预言家查验、医生救人)
│    ↓
│  白天阶段
│    ↓
│  公布夜间结果
│    ↓
│  讨论阶段 (AI 和玩家发言)
│    ↓
│  投票阶段
│    ↓
│  公布投票结果
│    ↓
└─ 检查胜利条件 → 如未结束返回夜晚
```

### 3. AI 行为流程
```
AI 收到游戏事件
  ↓
分析当前局势
  ↓
根据性格和角色决策
  ↓
执行行动 (发言/投票/夜间行动)
  ↓
等待下一个事件
```

## 技术架构

### 数据流
```
Mobile App (React Native)
  ↕ WebSocket
Backend Server (werewolf-live)
  ↕ WebSocket
OpenClaw Agents
```

### 状态管理
```typescript
// Zustand Store
interface GameState {
  room: Room | null;
  players: Player[];
  gamePhase: 'night' | 'day' | 'vote' | 'result';
  myRole: Role | null;
  messages: Message[];
  actions: Action[];
}

interface Room {
  id: string;
  name: string;
  config: GameConfig;
  status: 'waiting' | 'playing' | 'finished';
}

interface Player {
  id: string;
  name: string;
  isAI: boolean;
  isAlive: boolean;
  role?: Role; // 只有自己能看到
  status: 'ready' | 'not_ready';
}

interface Message {
  from: string;
  content: string;
  timestamp: number;
  type: 'chat' | 'system' | 'speech';
}
```

## AI 实现细节

### AI 决策引擎
```typescript
interface AIStrategy {
  // 夜间行动决策
  decideNightAction(gameState: GameState): Action;
  
  // 白天发言内容
  generateSpeech(gameState: GameState): string;
  
  // 投票决策
  decideVote(gameState: GameState): string; // playerId
  
  // 分析可疑度
  analyzeSuspicion(player: Player): number; // 0-100
}

class DetectiveStrategy implements AIStrategy {
  decideNightAction(state: GameState): Action {
    // 侦探型：优先查验发言最多的玩家
    const mostTalkative = this.getMostTalkativePlayer(state);
    return { type: 'check', target: mostTalkative.id };
  }
  
  generateSpeech(state: GameState): string {
    // 逻辑严密的发言
    return `基于前几轮的发言，我认为 ${target} 有 ${reason}...`;
  }
}

class AggressiveStrategy implements AIStrategy {
  decideVote(state: GameState): string {
    // 激进型：快速锁定目标
    return this.findMostSuspicious(state).id;
  }
  
  generateSpeech(state: GameState): string {
    // 强势发言
    return `我强烈建议投 ${target}，因为...`;
  }
}
```

### AI 性格配置
```typescript
interface AIPersonality {
  name: string;
  avatar: string;
  strategy: AIStrategy;
  speechStyle: SpeechStyle;
  talkFrequency: number; // 0-1, 发言频率
  suspicionThreshold: number; // 多疑程度
  logicalLevel: number; // 逻辑严密度
}

const personalities: AIPersonality[] = [
  {
    name: '福尔摩斯',
    avatar: '🕵️',
    strategy: new DetectiveStrategy(),
    speechStyle: 'logical',
    talkFrequency: 0.8,
    suspicionThreshold: 0.7,
    logicalLevel: 0.9,
  },
  {
    name: '战狼',
    avatar: '⚔️',
    strategy: new AggressiveStrategy(),
    speechStyle: 'aggressive',
    talkFrequency: 0.9,
    suspicionThreshold: 0.5,
    logicalLevel: 0.5,
  },
  // ... 更多性格
];
```

## 性能优化

### 1. WebSocket 优化
- 使用消息压缩
- 批量发送非紧急消息
- 心跳检测和自动重连

### 2. UI 优化
- 虚拟列表（大量玩家时）
- 图片懒加载
- 动画性能优化

### 3. 状态管理优化
- 选择性订阅（只更新相关组件）
- 使用 memo 避免不必要的重渲染

## 安全考虑

### 1. 防作弊
- 敏感信息服务端加密
- 角色信息只发给对应玩家
- 行动验证在服务端

### 2. 网络安全
- WebSocket 使用 wss:// (生产环境)
- Token 认证
- 防 DDoS

## 未来扩展

### 短期 (1-3 个月)
- [ ] 更多角色（女巫、猎人、白痴）
- [ ] 成就系统
- [ ] 排行榜
- [ ] 好友系统

### 中期 (3-6 个月)
- [ ] 语音聊天
- [ ] 自定义房间规则
- [ ] 复盘功能
- [ ] AI 难度调节

### 长期 (6-12 个月)
- [ ] 锦标赛模式
- [ ] 皮肤系统
- [ ] 观战直播
- [ ] 国际化

## 测试计划

### 单元测试
- AI 策略逻辑
- 游戏规则验证
- WebSocket 连接

### 集成测试
- 完整游戏流程
- 多玩家同步
- AI 行为测试

### 压力测试
- 100+ 并发房间
- WebSocket 稳定性
- 内存使用
